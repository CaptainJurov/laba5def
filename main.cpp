#include "colors.h"
#include <iostream>
#include <istream>
struct Звено {
    int a;
    float k;
    Звено* next;
};
struct math {
private:
    Звено* create(int n) {
    }
public:
    Звено first;
    math(std::iostream& is) {

    }
};